import Link from 'next/link';
import { notFound } from 'next/navigation';
import type { Metadata } from 'next';
import { getPublicJob } from '@/lib/public-recruitment';
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const job=await getPublicJob(slug);return job?{title:`${job.title} | Careers`,description:job.description.slice(0,155),alternates:{canonical:`/careers/${job.slug}`}}:{title:'Job not found | Careers'};}
export default async function PublicJobPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const job=await getPublicJob(slug);if(!job)notFound();return <main className="min-h-screen bg-surface-50"><div className="mx-auto max-w-5xl px-4 py-10 md:px-8"><Link href="/careers" className="text-sm font-medium text-brand-600 hover:underline">← All careers</Link><header className="mt-8 card p-7 md:p-10"><p className="text-sm font-semibold text-brand-600">{job.organizationName}</p><h1 className="mt-2 text-3xl font-semibold tracking-tight md:text-4xl">{job.title}</h1><div className="mt-5 flex flex-wrap gap-2"><span className="badge-gray">{job.workplaceType.replace('_',' ')}</span><span className="badge-gray">{job.employmentType.replace('_',' ')}</span>{job.location&&<span className="badge-gray">{job.location}</span>}{job.department&&<span className="badge-gray">{job.department}</span>}</div></header><div className="mt-6 grid gap-6 lg:grid-cols-[1fr_300px]"><article className="card p-7 md:p-10"><h2 className="text-xl font-semibold">About the role</h2><div className="mt-5 whitespace-pre-wrap text-[15px] leading-7 text-surface-700">{job.description}</div></article><aside className="card h-fit p-6"><h2 className="font-semibold">Apply for this role</h2><p className="mt-2 text-sm text-surface-500">Submit your application securely through the careers portal.</p>{job.closesAt&&<p className="mt-4 text-sm">Applications close <strong>{new Date(job.closesAt).toLocaleString()}</strong>.</p>}{job.applicationEnabled?<Link href={`/careers/${job.slug}/apply`} className="btn-primary mt-5 w-full justify-center">Apply now</Link>:<p className="mt-5 rounded-lg bg-surface-100 p-3 text-sm text-surface-600">Applications are currently closed.</p>}</aside></div></div></main>}
