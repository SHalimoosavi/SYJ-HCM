import Link from 'next/link';
import { notFound } from 'next/navigation';
import { getPublicJob } from '@/lib/public-recruitment';
import { PublicApplicationForm } from '@/app/careers/public-application-form';
export default async function ApplyPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const job=await getPublicJob(slug);if(!job)notFound();if(!job.applicationEnabled)return <main className="min-h-screen bg-surface-50"><div className="mx-auto max-w-xl px-4 py-16"><div className="card p-8 text-center"><h1 className="text-2xl font-semibold">Applications are closed</h1><p className="mt-2 text-sm text-surface-500">This role is no longer accepting applications.</p><Link href={`/careers/${slug}`} className="btn-secondary mt-6 inline-flex">Back to role</Link></div></div></main>;return <main className="min-h-screen bg-surface-50"><div className="mx-auto max-w-3xl px-4 py-10 md:px-8"><Link href={`/careers/${slug}`} className="text-sm font-medium text-brand-600 hover:underline">← Back to role</Link><div className="mt-6"><p className="text-sm font-medium text-brand-600">{job.organizationName}</p><h1 className="mt-1 text-3xl font-semibold">Apply: {job.title}</h1><p className="mt-2 text-sm text-surface-500">Your information is submitted directly into the organization’s ATS.</p></div><div className="mt-6 card p-6 md:p-8"><PublicApplicationForm slug={slug}/></div></div></main>}
