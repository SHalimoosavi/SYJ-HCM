import Link from 'next/link';
import { requireRole } from '@/lib/auth';
import { getRecruitmentDashboard, listRecruitmentStages } from '@/lib/recruitment-workflow';
export default async function RecruitmentPage(){const u=await requireRole('admin','hr');const d=getRecruitmentDashboard(u.organizationId);const stages=await listRecruitmentStages(u.organizationId);return <div className="max-w-7xl space-y-6"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm font-medium text-brand-600">Recruitment / ATS</p><h1 className="mt-1 text-2xl font-semibold">Recruitment workspace</h1><p className="mt-1 text-sm text-surface-500">Tenant-scoped hiring pipeline, interview operations and activity.</p></div><div className="flex flex-wrap gap-2"><Link className="btn-primary" href="/recruitment/interviews/new">Schedule interview</Link><Link className="btn-secondary" href="/recruitment/stages">Workflow settings</Link></div></div><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-5">{[['Awaiting screening',d.awaitingScreening],['Shortlisted',d.shortlisted],['Upcoming interviews',d.upcoming],['Feedback needed',d.feedbackNeeded],['Active stages',stages.filter((s:any)=>s.isActive).length]].map(([label,value])=><div className="card p-5" key={String(label)}><p className="text-xs font-medium uppercase tracking-wide text-surface-500">{label}</p><p className="mt-2 text-2xl font-semibold">{value}</p></div>)}</div><div className="grid gap-6 lg:grid-cols-2"><section className="card p-6"><div className="flex items-center justify-between"><div><h2 className="text-lg font-semibold">Applications by stage</h2><p className="text-sm text-surface-500">Real-time tenant-scoped counts.</p></div><Link className="btn-secondary" href="/recruitment/applications">Applications</Link></div><div className="mt-5 grid grid-cols-2 gap-3 sm:grid-cols-3">{d.applicationsByStage.map((r:any)=><div className="rounded-lg border border-surface-200 p-3" key={r.status}><p className="text-xs capitalize text-surface-500">{r.status.replaceAll('_',' ')}</p><p className="mt-1 text-xl font-semibold">{r.n}</p></div>)}</div></section><section className="card p-6"><div className="flex items-center justify-between"><div><h2 className="text-lg font-semibold">Recently updated applications</h2><p className="text-sm text-surface-500">Latest workflow activity.</p></div><Link className="btn-secondary" href="/recruitment/candidates">Candidates</Link></div>{d.recent.length===0?<p className="mt-5 text-sm text-surface-500">No applications yet.</p>:<div className="mt-4 space-y-2">{d.recent.map((r:any)=><Link className="block rounded-lg border border-surface-200 p-3 hover:bg-surface-50" key={r.id} href={`/recruitment/applications/${r.id}`}><div className="flex justify-between gap-3"><span className="font-medium">{r.application_reference}</span><span className="badge-amber">{r.status.replaceAll('_',' ')}</span></div><p className="mt-1 text-sm">{r.first_name} {r.last_name} · {r.title}</p></Link>)}</div>}</section></div></div>}
