'use client';
import { useActionState } from 'react';
import { updateDraftOfferAction } from '../offer-actions';
export default function OfferEditForm({offer}:{offer:any}){const [state,action,pending]=useActionState(updateDraftOfferAction,{error:null});return <form action={action} className="mt-5 space-y-4"><input type="hidden" name="offerId" value={offer.id}/><div className="grid gap-4 sm:grid-cols-2"><label className="label">Currency<input className="input" name="currency" defaultValue={offer.currency} maxLength={3}/></label><label className="label">Pay frequency<select className="input" name="payFrequency" defaultValue={offer.pay_frequency}><option value="annual">Annual</option><option value="monthly">Monthly</option><option value="weekly">Weekly</option><option value="hourly">Hourly</option></select></label><label className="label">Base salary (minor)<input className="input" name="baseSalaryMinor" defaultValue={offer.base_salary_minor}/></label><label className="label">Variable compensation (minor)<input className="input" name="variableCompMinor" defaultValue={offer.variable_comp_minor}/></label><label className="label">Joining bonus (minor)<input className="input" name="joiningBonusMinor" defaultValue={offer.joining_bonus_minor}/></label><label className="label">Allowances (minor)<input className="input" name="allowancesMinor" defaultValue={offer.allowances_minor}/></label><label className="label">Other compensation (minor)<input className="input" name="otherCompMinor" defaultValue={offer.other_comp_minor}/></label><label className="label">Joining date<input className="input" type="date" name="proposedJoiningDate" defaultValue={offer.proposed_joining_date||''}/></label><label className="label">Expiration date<input className="input" type="date" name="expiresAt" defaultValue={offer.expires_at?String(offer.expires_at).slice(0,10):''}/></label></div><label className="label">Benefits summary<textarea className="input min-h-24" name="benefitsSummary" defaultValue={offer.benefits_summary||''}/></label><label className="label">Employment terms<textarea className="input min-h-28" name="employmentTerms" defaultValue={offer.employment_terms||''}/></label>{state.error&&<p role="alert" className="rounded-lg bg-red-50 p-3 text-sm text-red-700">{state.error}</p>}<button className="btn-secondary" disabled={pending}>{pending?'Saving…':'Save draft changes'}</button></form>}
