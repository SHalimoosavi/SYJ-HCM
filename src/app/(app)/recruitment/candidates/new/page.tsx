import Link from 'next/link'; import { requireRole } from '@/lib/auth'; import { CandidateForm } from '../../forms';
export default async function NewCandidatePage(){await requireRole('admin','hr');return <div className="max-w-4xl space-y-6"><div><Link className="text-sm text-brand-600 hover:underline" href="/recruitment/candidates">← Candidates</Link><h1 className="mt-2 text-2xl font-semibold">Add candidate</h1><p className="mt-1 text-sm text-surface-500">Create a reusable candidate identity for one or more applications.</p></div><section className="card p-6"><CandidateForm/></section></div>}
